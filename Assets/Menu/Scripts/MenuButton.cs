using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MenuButton : MonoBehaviour
{
    public enum MenuEnum { NewGame, Continue, Quit, Options, Resume, MaineMenu };

    public MenuEnum WhatFonction;

    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}